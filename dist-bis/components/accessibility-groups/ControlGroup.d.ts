import { ReactNode } from 'react';
interface ControlGroupProps {
    title: string;
    children: ReactNode;
    textScaleFactor: number;
}
export declare function ControlGroup({ title, children, textScaleFactor }: ControlGroupProps): import("react/jsx-runtime").JSX.Element;
export {};
